import getPreviews from 'livarava-neuroner-mockups/utils/get-previews';
import { module, test } from 'qunit';

module('Unit | Utility | get previews');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = getPreviews();
  assert.ok(result);
});
