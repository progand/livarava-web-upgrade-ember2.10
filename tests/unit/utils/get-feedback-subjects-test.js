import getFeedbackSubjects from 'livarava-neuroner-mockups/utils/get-feedback-subjects';
import { module, test } from 'qunit';

module('Unit | Utility | get feedback subjects');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = getFeedbackSubjects();
  assert.ok(result);
});
