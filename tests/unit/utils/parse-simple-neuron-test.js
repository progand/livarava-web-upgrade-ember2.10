import parseSimpleNeuron from 'livarava-neuroner-mockups/utils/parse-simple-neuron';
import { module, test } from 'qunit';

module('Unit | Utility | parse simple neuron');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = parseSimpleNeuron();
  assert.equal(result, null);
});
