import getArticle from 'livarava-neuroner-mockups/utils/get-article';
import { module, test } from 'qunit';

module('Unit | Utility | get article');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = getArticle();
  assert.ok(result);
});
